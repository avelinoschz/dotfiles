# This file documents environment variables used by these dotfiles.
# It is safe to commit because values here are placeholders, not real secrets.
# Future hardening idea: manage secrets with gopass or encrypted files via sops/age.

CONTEXT7_API_KEY=__CHANGE_ME__
